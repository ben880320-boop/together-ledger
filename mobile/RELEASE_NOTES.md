## Together Ledger v1.3.8

## Together Ledger v1.3.19

### 記住裝置、安全撤銷與匿名可觀測性

- 已記住的 Firebase Email／Password 身分在 Web、PWA 或 Android 的一小時 App 工作階段到期後，會以既有的受保護 Firebase persistence 安全換發短效 App 工作階段；不保存明碼密碼。
- 暫時網路失敗不會清除已記住身分，只有主動登出、帳戶刪除，或 Firebase 明確回報 token 失效／撤銷時才會清除。Android 同時覆蓋未授權與 `auth.me` 暫無使用者的冷啟動路徑，且只會嘗試一次靜默恢復。
- Web-only 管理介面新增非管理員帳戶的「撤銷所有登入」保護確認；後端會同時遞增 App `sessionVersion` 與撤銷 Firebase refresh tokens，管理員自身與其他管理員帳戶均受保護。
- 新增沒有 Email、Firebase UID、帳本、交易、金額或 token 的匿名作業事件彙總，用於檢視記住裝置恢復、session 撤銷與同步衝突的成功／失敗趨勢。

### 發布

- Android 版升級為 `versionName=1.3.19`、`versionCode=42`，維持固定受保護簽章與 SHA-256 校驗檔。

## Together Ledger v1.3.18

### 帳戶刪除防誤觸與告別互動

- Web、PWA 與 Android 的刪帳操作改為一致的兩階段流程：輸入目前密碼後，先顯示五秒倒數的最終確認。
- 倒數只解除「永久刪除帳號」按鈕鎖定，**不會自動送出刪除**；使用者仍可隨時取消並保留帳號。
- 刪除成功後會清除本機與 Firebase 工作階段，安全返回登入入口，並顯示一次性、可關閉的友善告別訊息，不保留已刪帳戶的敏感狀態。

### 發布

- Android 版升級為 `versionName=1.3.18`、`versionCode=41`，維持固定受保護簽章與 SHA-256 校驗檔。

## Together Ledger v1.3.17

### 帳戶刪除修復

- 修正 Web 與 Android 的 Firebase Email／Password 帳戶即使輸入正確密碼、完成近期驗證後，仍被伺服器誤判為不支援密碼自刪的問題。
- Firebase Email／Password 帳戶現在會正確進入近期驗證與帳戶資料清理流程；Google 等非密碼登入帳戶仍會被拒絕以密碼刪除，維持原有帳戶保護界線。
- 新增 server 契約與 Android 核心流程回歸測試，覆蓋 Firebase Email／Password 自刪資格、非密碼帳戶拒絕及既有工作階段撤銷條件。

### 發布

- Android 版升級為 `versionName=1.3.17`、`versionCode=40`，維持固定受保護簽章與 SHA-256 校驗檔。

## Together Ledger v1.3.16

### 帳戶生命週期與跨平台安全

- 修正 Web／PWA 的「記住此裝置」登入交換會遺漏 `rememberDevice` 參數的問題；勾選後 Firebase persistence 與伺服器 HTTP-only cookie 均採 30 天受保護工作階段，未勾選則維持瀏覽器／冷啟動工作階段，且不保存明碼密碼。
- Android、Web 與 PWA 的忘記密碼與重寄驗證改為先開啟 Email 對話框；重寄流程另要求目前密碼，以降低未授權的驗證信濫用。註冊前顯示必須手動關閉的 24 小時未驗證身分清理提醒。
- 個人設定新增近期 Firebase 密碼驗證後的新信箱驗證流程。驗證連結完成後，使用者以新信箱重新登入，伺服器只同步同一 Firebase UID 的已驗證信箱並撤銷舊 session。
- 既有本機帳密入口改為 30 天低顯著遷移連結；到期後伺服器明確拒絕舊密碼回退，避免繞過 Firebase 的驗證、重設與撤銷控制。

### Web 專用管理保護與清理準備

- Web 新增僅限管理員的帳戶管理頁，但帳戶摘要、搜尋、稽核與刪除均由後端 `adminProcedure` 驗證。介面不顯示密碼、Firebase UID、交易或收據資料；管理員不可刪除自己或其他管理員，刪除必須輸入 `DELETE` 並寫入稽核紀錄。
- 新增每日未驗證 Firebase Email／Password 身分清理 handler。它僅處理超過 24 小時、尚未驗證、且僅存在於 Firebase 的身分，不會刪除既有共帳帳戶或帳本資料；正式每日排程會在部署完成並經管理員確認後啟用。

### 發布與安全性

- Android 版升級為 `versionName=1.3.16`、`versionCode=39`，維持固定受保護簽章與 SHA-256 校驗檔。

## Together Ledger v1.3.15

### 安全記住此裝置

- Android App、Web 與 PWA 的 Firebase 登入頁新增「記住此裝置」選項。勾選後只會保存受保護的工作階段憑證，以便下次開啟時安全恢復登入；**絕不保存或自動填入明碼密碼**。
- 未勾選時，Web／PWA 只保留目前瀏覽器工作階段；Android 於下次冷啟動前會撤銷可恢復狀態，需重新輸入電子信箱與密碼。
- 主動登出、帳戶刪除或伺服器的 session version 失效時，會一併清除記住此裝置偏好與本機工作階段，避免舊帳戶被靜默恢復。

### 發布與安全性

- Android 版升級為 `versionName=1.3.15`、`versionCode=38`，維持固定受保護簽章與 SHA-256 校驗檔。

## Together Ledger v1.3.14

### Firebase 認證提示與繁體中文郵件

- Android App、Web 與 PWA 的註冊、重寄驗證信、忘記密碼與同信箱綁定改用一致、可關閉的浮動提示，成功與失敗狀態不再容易被表單內容淹沒。
- 忘記密碼只需先輸入電子信箱；送出後以不揭露帳戶是否存在的通用訊息引導使用者查看收件匣與垃圾郵件匣，並保留重新寄送與返回登入入口。
- Firebase 驗證信與重設信在用戶端指定繁體中文語系 `zh-TW`。Firebase Console 可固定採用「共帳 Together Ledger」寄件者名稱與簡短主旨，詳細手動設定見 `docs/firebase-email-template-localization.md`。

### 發布與安全性

- Android 版升級為 `versionName=1.3.14`、`versionCode=37`，維持固定受保護簽章、Firebase client 公開設定注入及 SHA-256 校驗檔。

## Together Ledger v1.3.13

### Firebase 電子郵件帳號安全

- Android App、Web 與 PWA 統一採用 Firebase Authentication Email／Password：新帳號註冊後必須先完成 Email 驗證，登入頁提供重寄驗證信與不揭露帳號存在與否的忘記／重設密碼入口。
- 既有本機帳密使用者可從個人設定以相同、已驗證的電子信箱綁定 Firebase；此過程不變更既有使用者 ID、帳本成員資格、交易或設定。
- Firebase 登入使用一小時短效 app session，JWT 以 session version 集中驗證；綁定時會撤銷舊 session，Android 可在 Firebase 已驗證身份有效時安全自動換發。
- Firebase 已綁定帳戶的刪除流程要求重新輸入 Firebase 密碼，伺服器再驗證五分鐘內的 ID Token、UID 與相同電子信箱後移除 app 與 Firebase identity。

### 發布與安全性

- Android 版升級為 `versionName=1.3.13`、`versionCode=36`。APK 建置僅注入 Firebase client 公開設定；服務帳號憑證保持在伺服器端，不會寫入 APK。

## Together Ledger v1.3.12

### 鍵盤穩定性與月結算可見性

- Android 改由原生 `resize` 管理鍵盤開啟與收合時的畫面尺寸；共用輸入容器不再在 Android 疊加 JavaScript 高度避讓，避免鍵盤關閉後重複上、下位移，同時保留原生輸入欄可見性。
- Android App、Web 與 PWA 的帳本總覽新增醒目的「本月待結算」摘要，清楚顯示月份、付款方向、差額與待確認／已鎖定／已重新開啟狀態，原有提出、第二位成員確認、管理員重開與版本比對保護不變。
- 結算歷史可依月份及狀態篩選，直接查閱待確認、已鎖定與已重新開啟的每月結算快照；資料仍由帳本成員授權的既有 tRPC 端點取得。

### 發布與安全性

- Android 版升級為 `versionName=1.3.12`、`versionCode=35`，延續 GitHub Actions 固定受保護簽章與 SHA-256 校驗檔。

## Together Ledger v1.3.10

### 診斷偏好與讀取體驗

- 修正首次開啟「協助改善 App：傳送錯誤診斷」時，讀取與寫入偏好同時發生可能造成儲存失敗的競態問題；設定頁會提供可見錯誤與重新嘗試入口。
- Android 合併重複帳本載入請求；Web/PWA 優先使用 SSE 同步並減少不必要的正規化與快取失效。骨架載入採低干擾動畫，並尊重減少動態效果偏好。

### 發布與安全性

- Android 版升級為 `versionName=1.3.10`、`versionCode=33`，延續 GitHub Actions 固定受保護簽章與 SHA-256 校驗檔。

## Together Ledger v1.3.9

### Android 帳本設定穩定性與隱私診斷

- 修正 Android 進入帳本設定時，分類或支付方式圖示選擇器可能造成閃退的問題；設定元件另有局部復原入口。
- 「協助回報技術錯誤」預設關閉，僅在個人設定明確同意後傳送去識別化技術診斷；不會傳送帳本、收支、邀請碼、帳號、電子信箱、密碼、憑證或收據圖片。

### 發布與安全性

- Android 版升級為 `versionName=1.3.9`、`versionCode=32`，延續 GitHub Actions 固定受保護簽章與 SHA-256 校驗檔。

## Together Ledger v1.3.8

### 分類與支付方式圖示選擇器修正

- Android App、Web 與 PWA 的新增／編輯分類及支付方式，現在固定顯示「不使用圖示」；選取後會保留真正的無圖示值，不會顯示預設圖案。
- 圖示選擇器先顯示常用項目，可再展開完整圖示庫並以「車」、「旅行」、「愛心」等中文關鍵字搜尋。

### 發布與安全性

- Android 版升級為 `versionName=1.3.8`、`versionCode=31`，延續 GitHub Actions 固定受保護簽章與 SHA-256 校驗檔。

## Together Ledger v1.3.2

## Together Ledger v1.3.7

### 帳本圖示選擇器

- Android App、Web 與 PWA 的建立帳本與帳本設定皆將「無圖示」固定顯示在選擇器最前方，不必先展開清單即可選取。
- 展開完整圖示庫後可使用中文關鍵字或表情符號搜尋，例如「愛心」、「車」、「旅行」、「房屋」與「購物」；三端共用同一份 70 項圖示與搜尋規則。
- 選擇「無圖示」後，帳本卡片與切換入口不會以預設圖示取代使用者選擇。

### 發布與安全性

- Android 版升級為 `versionName=1.3.7`、`versionCode=30`，維持 GitHub Actions 固定受保護簽章與 SHA-256 校驗檔；已安裝官方 v1.3.6 的裝置可直接覆蓋更新。

## Together Ledger v1.3.5

### 跨平台即時同步

- **SSE 帳本事件流**：Android App、Web 與 PWA 現在以可重連的 Server-Sent Events 接收帳本異動提示；新增、編輯、刪除收支，以及預算、分類、支付方式、儲蓄桶、帳本名稱、圖示、成員與權限異動，會在另一端自動重新取得相關資料。
- **安全同步界線**：事件流僅傳遞帳本游標與異動類型，不會傳送收支金額或收據內容。端點以登入憑證與帳本成員資格驗證，並保留斷線後重連、心跳與手動重新整理路徑。
- **草稿保護延續**：Web／PWA 以防抖快取失效更新，Android 則在背景重新載入工作區；既有開啟中的輸入草稿不會因同步提示被直接覆寫。

### 帳本圖示庫

- 建立帳本與帳本設定共用同一套 **70 個圖示選項**，新增家庭、交通、旅行、理財、生活、健康、學習、工作與自然情境圖示；仍可選擇「無」圖示。

### 發布與安全性

- Android 版升級為 `versionName=1.3.5`、`versionCode=28`，維持 GitHub Actions 固定受保護簽章與 SHA-256 校驗檔。已安裝 v1.3.4 的裝置可在相同簽章下直接覆蓋更新。

## Together Ledger v1.3.4

### P0 跨平台體驗改善

- **網頁版與 PWA 行動操作一致化**：行動裝置的底部導覽、觸控安全區、表單與同步提示已對齊 Android 的操作節奏，降低跨裝置切換的差異。
- **草稿與同步狀態中心**：帳本工作區會清楚呈現同步中、已同步、離線／失敗與最後同步時間；失敗時保留草稿，並提供可見的重試入口。
- **PWA 安全更新**：偵測到新版時，若仍有未提交的草稿，會先提示使用者保留內容，再選擇立即重新載入或稍後更新，避免更新中斷輸入。
- **Android 更新安裝診斷**：更新專區新增目前／可用版本、官方下載來源、下載進度、SHA-256 校驗、安裝狀態與簽章衝突處理指引。
- **Android 鍵盤安全表單**：登入、收支、預算、固定收支、帳本與儲蓄桶等可輸入畫面採用共用鍵盤安全容器，改善欄位被遮擋及關閉鍵盤後的版面跳動。

### 發布與安全性

- Android APK 維持以 GitHub Actions 的固定受保護簽章建置，Release 同時附上 SHA-256 校驗檔。若裝置上仍有由舊簽章建立的歷史安裝，首次改版可能需先移除舊 App 後安裝本版；後續同簽章版本可直接覆蓋更新。

## Together Ledger v1.3.3

### 儲蓄桶額外存入與收支類型保護

- Android App、網頁版與 PWA 的儲蓄桶皆可由使用者在有可用餘額時執行「額外存入」。金額以整數分保存，會從該桶已設定的扣款支付方式轉存，並立即同步更新目標進度與三端畫面。
- 每次額外存入都會建立不可編輯、不可刪除的正式轉存與分配稽核紀錄，會清楚標示為「手動額外存入」；自動每月分配則維持原有的「自動分配」來源標示。
- 額外存入會檢查帳本權限、支付方式可用餘額與儲蓄桶版本。若餘額不足或其他成員已更新同一目標，畫面會保留輸入並提供明確錯誤或重新整理入口，不會靜默覆寫。
- 網頁一般「新增／編輯收支」現在只提供收入與支出。轉帳僅能由受保護的儲蓄桶自動／手動轉存流程建立，避免一般收支誤建系統轉存資料。

## Together Ledger v1.3.1

### 儲蓄目標完成與封存

- 當儲蓄桶首次達標時，Android App、網頁版與 PWA 會顯示專屬慶祝提示；使用「減少動態效果」時會保留訊息而不播放非必要動畫。
- 已達標目標可選擇封存／隱藏，封存會停止日後自動分配但不刪除目標、正式轉存、分配紀錄或操作稽核；可隨時重新顯示並還原管理。

### 同步草稿保護

- 背景同步與工作區重新載入不會再覆寫任何開啟中的新增／編輯表單草稿。此保護涵蓋收支、預算、固定收支、出遊規劃、分類、支付方式、帳本設定與儲蓄桶。

## Together Ledger v1.3.0

### 儲蓄桶與正式資金轉存

- Android App、網頁版與 PWA 的「規劃」頁新增儲蓄桶。可不限數量建立買車基金、旅行、房屋頭期款或自訂目標，並設定目標金額、每月固定分配額度、扣款支付方式、每月分配日期與優先順序。
- 系統每日檢查到期桶後，會依優先順序以正式 `transfer` 交易記錄轉存；若可用餘額不足，將部分分配並記錄不足金額。轉存不列入收入、支出或消費分析。
- 每個桶都可查看月份、預定金額、實際分配金額、不足金額與完成／部分分配／略過狀態。系統產生的轉存交易為唯讀，需從儲蓄桶設定調整後續分配。

### 共同編輯保護

- 儲蓄桶更新與暫停均使用版本比對。若其他成員先完成異動，畫面會要求重新整理後再編輯，不會靜默覆寫對方設定。
- 正在使用的扣款支付方式不可被隱藏或刪除，避免排程失去有效資金來源。

## Together Ledger v1.2.9.2

### 通知功能暫停

- Android App 與網頁版已暫時移除通知偏好、通知狀態、推播裝置註冊、收支提醒，以及預算 80%／100% 門檻提醒；伺服器亦不再建立站內通知或投遞 Expo 推播。
- 本次僅停用功能與原生通知權限，既有帳本、交易、預算與資料表均保留，待完成 FCM 憑證設定後可重新規劃啟用。

### 資料同步保護

- 網頁新增或編輯收支對話框會在開啟期間固定工作區快照，背景同步不會再清除尚未提交的輸入內容或將選項重設為預設值。

## Together Ledger v1.2.9.1

### 帳本與即時資料

- 帳本圖示選擇器擴充為 36 種圖示與「無」選項，並可在帳本設定中和名稱一起調整；變更會同步反映於 Android App 與網頁。
- 新增、編輯與移除收支會立即更新目前畫面與工作區快取，並以背景同步和失敗回復降低等待與舊資料殘留。

### 預算提醒與通知診斷

- 月總預算與分類預算可個別啟用 80% 及 100% 門檻。達標時會建立可連回對應支出內容的帳內提醒，並依使用者偏好投遞 Android 推播。
- 個人設定新增通知狀態診斷，顯示裝置註冊、最近投遞時間與失敗原因，並提供重新整理及在 Android App 重新註冊的入口。
- Android 登入後會重新註冊通知裝置；伺服器會使用正確通知通道與高優先順序投遞。網頁通知診斷同時改為正確顯示後端記錄的投遞失敗原因。

### 安全與取得方式

- APK 僅透過官方 GitHub Release 發布，並附帶 SHA-256 檢查碼；Android 系統會在安裝前顯示確認流程。
## Together Ledger v1.3.11

### 跨平台邀請、每月結算與讀取效能

- 帳本設定的邀請 QR Code 改為 HTTPS 通用邀請頁：Android 已安裝 App 時會優先開啟 App；未安裝 App、使用 Web／PWA、iOS 或電腦時，可先閱讀邀請說明，再登入後安全加入帳本。
- 結算升級為每月快照：一位成員提出當月未結清差額，第二位成員確認後才鎖定該月份；帳本管理員可明確重新開啟，修正後需再次提出與確認。
- 工作區縮減不必要的快取失效、重複請求與衍生資料重算，同時保留 SSE、節流輪詢備援、離線快照及未提交草稿保護。

### 發布與安全性

- Android 版升級為 `versionName=1.3.11`、`versionCode=34`，延續 GitHub Actions 固定受保護簽章與 SHA-256 校驗檔。
