# 網頁正式入口驗證紀錄

## 2026-08-19：舊版導向修正

已於正式網域 `https://togetherapp-hdbmsjkf.manus.space/` 驗證首頁顯示 **v1.2.8.7** 的新版產品入口與更新歷程。

另以 `?legacyWorkspacePreview=1&release=6e893cf8` 重新載入正式網域。正式環境仍固定顯示新版入口，未載入舊版靜態模擬帳本，證明舊版視覺已被限制在本機開發模式，既有登入工作階段與查詢參數均無法使正式網站回退。

已完成的技術驗證：

- 46 項 Vitest 測試通過。
- TypeScript `tsc --noEmit` 通過。
- Vite 生產建置通過。
- 開發預覽與正式網域皆顯示 v1.2.8.7 產品入口。

## 2026-08-19：網頁帳本與版本透明度

已完成新版網頁帳本的桌面與 390px 手機寬度視覺驗證。未登入使用者會先看到帳密登入引導；登入後若尚無帳本，會顯示建立首個帳本與使用邀請碼加入的空狀態；已有帳本時，系統會從既有 tRPC 工作區資料顯示結算、最近收支、分析與新增收支入口。

網站頁尾會顯示網頁版本、由 `__manus__/version.json` 取得的台北發布時間與短版建置識別碼。重新載入最新版本按鈕採用 `cache: "reload"` 請求並使用目前網址重新導向，避免遺留前端資源快取。

手機檢查結果：首頁、空帳本工作區與頁尾的版本資訊皆保持可讀，按鈕未超出 390px 螢幕邊界。

## 2026-08-19：跨平台帳本工作區重構

已於桌面預覽檢查首頁、電子信箱／密碼登入頁及 `/app` 受保護帳本入口。

- 產品首頁顯示 Android 與網頁版同步使用的說明、版本資訊及手動重新載入最新版本控制。
- 登入頁清楚標示使用 Android App 相同的電子信箱與密碼，未另建一套帳號系統。
- 未登入狀態的帳本路由會先確認工作階段，不會洩漏共同帳本資料；登入完成後才會載入真實 tRPC 帳本工作區。
- 型別檢查、49 項 Vitest 測試與 Vite 生產建置均已通過。

## 2026-08-19：認證載入狀態待修正

重啟開發服務後，未帶本機工作階段的 `/app` 路由仍持續顯示「正在確認登入狀態⋯」，而非切換至帳密登入引導。伺服器已記錄 `Missing session cookie`，因此此問題限定於前端認證查詢完成後的狀態收斂，將於發布完整網頁帳本前修正。

## 2026-08-19：跨平台服務與登入入口恢復

正式網域 `https://togetherapp-hdbmsjkf.manus.space/?release=079c29ed` 已回傳 HTTP 200，並顯示最新產品入口、版本 `v1.2.8.7`、台北發布時間與「重新載入最新版本」操作。這確認先前的 Cloud Run 404／沒有啟用中部署問題已解除。

由產品首頁進入 `https://togetherapp-hdbmsjkf.manus.space/login` 後，正式登入頁正確顯示電子信箱、密碼、登入與註冊帳號入口，並明確說明 Android App 與網頁版使用相同帳本資料。驗證中未輸入任何使用者帳密，因此未改動帳戶或帳本資料。

## 2026-08-19：重新載入與發布時間驗證

正式登入頁可在帶有部署識別的網址下正常渲染，並顯示電子信箱／密碼登入入口及「重新載入最新版本」控制。先前重新載入導向 `about:blank` 的問題已定位至瀏覽器導向方式，修正後會保留目前完整網址。

另發現正式環境的 `/__manus__/version.json` 會由單頁應用程式回退並回傳 HTML，而非 JSON 中繼資料。因此頁尾不能將該路徑作為發布時間的唯一來源；後續改採 Vite 建置時產生的時間戳，讓每次部署都帶有可讀、可驗證的發布時間。
